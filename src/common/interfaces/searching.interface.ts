/**
 * Interface for searching
 *
 * @interface ISearching
 * @property {string[]} fields - fields for searching
 * @property {string} search - search
 */
export interface ISearching {
  fields: string[];
  search: string;
}
